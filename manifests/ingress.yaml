apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: get-task-api-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: get-task-api.memesforlife.sbs
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: todo-ui
            port: 
              number: 80
